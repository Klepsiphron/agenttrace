/** Current CLI release version. */
export const VERSION = '0.0.0';

/** Published npm package name. */
export const PACKAGE_NAME = '@agenttrace/cli';

// NOTE: Full CLI implementation (init, dashboard, export, stats) is out of scope for Task 5 (Dashboard frontend).
// Partial code was removed to keep the monorepo build/test/lint green.
