[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agenttrace-io-middleware-crewai"
version = "0.4.19"
description = "CrewAI middleware for AgentTrace - automatic task and tool tracing"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [
  { name = "Klepsiphron" }
]
keywords = ["ai", "agents", "observability", "tracing", "llm", "crewai"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
  "agenttrace-io>=0.1.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
]
crewai = [
  "crewai>=0.28.0",
]

[project.urls]
Homepage = "https://github.com/Klepsiphron/agenttrace"
Repository = "https://github.com/Klepsiphron/agenttrace"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"
